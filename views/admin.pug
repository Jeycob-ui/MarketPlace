extends layout

block styles
  link(rel="stylesheet" href="/admin.css")

block content
  h1 Panel de administración

  // ===== Usuarios =====
  h2 Usuarios
  ul
    each u in users
      li #{u.id} - #{u.name} (#{u.email}) - #{u.role}
